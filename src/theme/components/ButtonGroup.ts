import { Components } from '@mui/material';

const ButtonGroup: Components['MuiButtonGroup'] = {
  styleOverrides: {
    root: {
      borderRadius: '0.5rem',
    },
  },
};

export default ButtonGroup;
