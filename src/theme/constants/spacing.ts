// Reference spacing scale (in px). MUI's default spacing unit is 8px,
// so these values correspond to theme.spacing(0.5), spacing(1), spacing(2), spacing(3), spacing(4).
const spacing = {
  xs: 4,
  sm: 8,
  md: 16,
  lg: 24,
  xl: 32,
} as const;

export default spacing;
